{ "name":"word-highlighter","version":"0.1.0","private":true,"type":"module",
  "scripts":{"build":"tsc --noEmit && vite build", "zip":"npm run build && cp manifest.json dist/ && cp -r _locales dist/ && cp -r icons dist/ && mkdir -p release && rm -f release/word-highlighter.zip && (cd dist && zip -r ../release/word-highlighter.zip .)"},
  "devDependencies":{"typescript":"^5.5.4","vite":"^5.4.0","@types/chrome":"^0.0.270"} }
